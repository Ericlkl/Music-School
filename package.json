{
  "name": "music-school",
  "version": "1.0.0",
  "description": "This is a profolio project for building full stack react application. Currently, the prject using MERN Stack to build the whole project. In the final stage, The backend system might switch to use GraphQL and apollo server.",
  "main": "index.js",
  "scripts": {
    "client": "npm run start --prefix client",
    "client-install": "npm install --prefix client",
    "client-build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "server": "npm run server --prefix server",
    "server-install": "npm install --prefix server",
    "dev": "concurrently 'npm run client' 'npm run server' "
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ericlkl/Music-School.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Ericlkl/Music-School/issues"
  },
  "homepage": "https://github.com/Ericlkl/Music-School#readme",
  "dependencies": {
    "concurrently": "^5.0.2"
  }
}
